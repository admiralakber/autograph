{
  "name": "autograph-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Autograph — a network that learns to draw its own beginning. The live, client-side strange-loop demo.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "node --experimental-strip-types scripts/smoke.ts",
    "swarm-live": "node --experimental-strip-types scripts/swarm-live.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/three": "^0.184.1",
    "@webgpu/types": "^0.1.70",
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  },
  "dependencies": {
    "hsluv": "^1.0.1",
    "three": "^0.184.0"
  }
}
